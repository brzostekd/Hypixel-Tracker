import { getStats } from "../../getStats";
import logo from '../../assets/bed.png';

export const Player = () => {
  const { bedwarsStats, playerInfo } = getStats();

  return (
    <>
      <div className="w-screen h-screen bg-purple-500">
        
      </div>
    </>
  );
};
